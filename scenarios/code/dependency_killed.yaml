name: dependency_killed
domain: code
description: Pause a downstream dependency and verify the service degrades gracefully

fault:
  type: dependency_killed
  service: ddagent

baseline_seconds: 10
observation_seconds: 25
recovery_seconds: 15

compliance_tags:
  - code.dependency_resilience
